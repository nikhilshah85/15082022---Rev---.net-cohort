import { EmpsaluatationPipe } from './empsaluatation.pipe';

describe('EmpsaluatationPipe', () => {
  it('create an instance', () => {
    const pipe = new EmpsaluatationPipe();
    expect(pipe).toBeTruthy();
  });
});
