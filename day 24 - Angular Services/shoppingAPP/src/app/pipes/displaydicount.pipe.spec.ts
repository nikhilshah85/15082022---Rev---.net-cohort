import { DisplaydicountPipe } from './displaydicount.pipe';

describe('DisplaydicountPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplaydicountPipe();
    expect(pipe).toBeTruthy();
  });
});
